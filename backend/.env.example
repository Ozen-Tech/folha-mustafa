# Local: use PostgreSQL (ex.: Docker ou instalação local)
# Render: use a Internal Database URL do PostgreSQL criado no painel
DATABASE_URL="postgresql://user:password@localhost:5432/folha_mustafa"
JWT_SECRET="change-me-in-production"
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
